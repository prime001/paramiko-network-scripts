Waiting for permission to write the file. Once approved, the script will be at `/opt/NetAutoCommitter/bgp_neighbor_check.py`.

The script is a BGP neighbor state monitor — distinct from the existing `routing_table.py`/`routing_table_v2.py` (which dump the FIB). Key features: VRF support, `--down-only` filter for alerting use, JSON output, and a non-zero exit code when any peer is down so it integrates cleanly with monitoring systems.